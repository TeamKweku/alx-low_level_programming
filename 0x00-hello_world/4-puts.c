#include <stdio.h>
/**
  * main - This progam outputs a stringed text to the stdout
  * with the print function puts
  *Return: the return function return 0 for success
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
