#include "main.h"
/**
 * _abs - a function that returns the absolute value
 * @n: the number to be checked
 *
 * Return: positive integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
