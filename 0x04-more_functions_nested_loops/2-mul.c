#include "main.h"
/**
 * mul - function takes two integers and returs their product
 * @a: first variable
 * @b: second variable
 *
 * Return: the product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
